package com.example.foodflow.helpers;

import java.util.function.Predicate;

/* 
public class StringFilter extends Filter<String> {

    private StringFilter(String value, Predicate<String> operation) {
        super(value, operation);
    }

    // -------------------- String-specific operations --------------------

    public static StringFilter contains(String substring) {
        return new StringFilter(substring, s -> s.contains(substring));
    }

    public static StringFilter notContains(String substring) {
        return new StringFilter(substring, s -> !s.contains(substring));
    }

    public static StringFilter startsWith(String prefix) {
        return new StringFilter(prefix, s -> s.startsWith(prefix));
    }

    public static StringFilter endsWith(String suffix) {
        return new StringFilter(suffix, s -> s.endsWith(suffix));
    }

    public static StringFilter matches(String regex) {
        return new StringFilter(regex, s -> s.matches(regex));
    }

    // -------------------- Base comparison operations --------------------

    public static StringFilter equal(String value) {
        return new StringFilter(value, s -> s.equals(value));
    }

    public static StringFilter notEqual(String value) {
        return new StringFilter(value, s -> !s.equals(value));
    }

    public static StringFilter greaterThan(String value) {
        return new StringFilter(value, s -> s.compareTo(value) > 0);
    }

    public static StringFilter greaterThanOrEqual(String value) {
        return new StringFilter(value, s -> s.compareTo(value) >= 0);
    }

    public static StringFilter lessThan(String value) {
        return new StringFilter(value, s -> s.compareTo(value) < 0);
    }

    public static StringFilter lessThanOrEqual(String value) {
        return new StringFilter(value, s -> s.compareTo(value) <= 0);
    }
        
}
    */

